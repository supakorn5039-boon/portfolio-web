/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.jsdelivr.net', 'tailwindcss.com', 'cdn-icons-png.flaticon.com', 'flagcdn.com'],
  },
};

export default nextConfig;
